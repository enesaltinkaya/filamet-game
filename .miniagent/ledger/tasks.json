[
  {
    "id": 1,
    "desc": "Regenerate the leaf texture as ONE dense cluster (NOT an edge-to-edge field): rewrite scripts/make_leaf_sprite.py for a 256px canvas — top ~80 rows = fully opaque bark band where kBarkUvRect samples; leaf region below = one dense leaflet cluster (~150-250 leaflets, 6-14px, varied hue 78-158/sat/val, ~70-85% core fill, fluffy outline) surrounded by a guaranteed transparent moat (alpha < 128, >= ~8px) around the whole region border, so every sampled rect's cutout silhouette is the cluster outline, never a rectangle. Keep the seeded RNG, extend self-checks (bark band rows 100% opaque, moat clear, core coverage >= 0.6), run it, verify the PNG by eye, then repack (scripts/data.sh, cwd c-game, BIN_DIR=build/c-game).",
    "status": "pending",
    "result": null
  },
  {
    "id": 2,
    "desc": "Densify the canopy (config + emitCards, no texture dependency): in c-utils/treegen/TreeGen.cpp configDeciduous() raise maxTris 3300 -> 6000 (MANDATORY: 15 cards x 32 tips x 4 tris exceeds the 3300 cap and Gen::room in TreeGen.cpp:86 silently clips terminal foliage), cardCountMin 1 -> 11, cardCountMax 2 -> 15, keep cardSize ~0.055 (5.5% of height, matches original 5-6%). In emitCards (~TreeGen.cpp:302-307) replace the random 45-90% sub-rect sampling with whole-region sampling (fw=fh=1.0, optional 0.9-1.0 centered crop for variety) so each card's cutout silhouette is the full cluster, never an arbitrary rectangle crop. Verify: 'azgaarPropMesh build' log shows deciduous tri count ~3600-4100 with validation PASS, and the frame-1200 screenshot shows a visibly denser canopy (still coarse until task 1 lands — acceptable for this task).",
    "status": "done",
    "result": "maxTris 6000, cardCount 11/15, whole-region centered-crop sampling (fw=fh 0.9-1.0); deciduous ~4500-4600 tris (< 6000 cap, Gen::room never clips), validation PASS; canopy now a dense crown with irregular silhouette — cards still show old sparse ash.png content (task 1)."
  },
  {
    "id": 3,
    "desc": "Align UV rects with the regenerated texture: update kLeafUvRect/kBarkUvRect in c-utils/treegen/TreeGen.h to the new 256px ash.png layout under the engine's v=0=image-top convention (bark rect fully inside the opaque bark band; leaf rect fully inside the cluster region INCLUDING the moat so no card sample touches the border), and confirm the DECIDUOUS AZGAAR_PROPS_FLAG_ALPHA_TEST cutout path (props_ps.hlsl alpha-discard + shadow pass) still discards the transparent moat — no renderer changes expected. Depends on task 1 (needs the new layout).",
    "status": "pending",
    "result": null
  },
  {
    "id": 4,
    "desc": "Screenshot A/B until it matches: run the pinned verification command from plan.md, compare /tmp/tree_after.png against /var/home/enes/Downloads/tree.png — canopy must read as a dense natural leaf tangle with a fluffy outer silhouette and NO square card silhouettes at the framed close-up range (compare canopy character, not pixel positions — camera auto-frames a different cell each run); confirm trunk bark, grass cards, and conifer/acacia/shrub (other treegen strategies) are unaffected, and the 'azgaarPropMesh build' log line shows validation PASS with sane deciduous tri counts. Depends on tasks 1+3.",
    "status": "pending",
    "result": null
  }
]
