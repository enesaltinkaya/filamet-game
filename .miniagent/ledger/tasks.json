[
  {
    "id": 1,
    "desc": "Baseline RenderDoc profile: scripts/run.sh renderdoc capture at a tree-dense cell, scripts/rdc.py list + dump the props pass, plus an ENGINE_SCREENSHOT baseline shot at the same cell/camera/seed (the A/B reference for tasks 2/3); record the props-pass GPU time and draw-level breakdown in .miniagent/ledger/notes.md.",
    "status": "done",
    "result": "Baseline capture /tmp/RenderDoc/c-game_frame300.rdc (598 MB, frame 300, Chilerel 2026-08-11-15-35.map, camera -303.1/5.0/-427.2 yaw-28 pitch-17) + A/B ref shot /tmp/treeopt_baseline_frame300.jpg; props pass = ~44% of per-pass event time, deciduous = 98% of submitted props tris (near 46% + far 52%), tree cards re-rasterized 3x in shadow; frame is tri-bound not fill-bound (rasterized = 37% of submitted) - full draw-level breakdown in notes.md round 2, tooling scripts/rdc_props_pass.py."
  },
  {
    "id": 2,
    "desc": "Cut the near-LOD deciduous mesh in c-utils/treegen/TreeGen.cpp (configDeciduous: cardCountMin/Max per tip and/or tip/branch structure, leafSeg/leafRing) until all 4 variants are <=9999 tris (baseline 28344 = full untruncated model: bands 9144 + cards 19200), keeping the canopy silhouette (baseline A/B shows a FULL untruncated canopy per round-1 correction, so judge on silhouette extent); screenshot A/B vs /tmp/treeopt_baseline_frame300.jpg using the exact repro env vars in notes.md round 2 (ENGINE_SCREENSHOT_FRAME=300, same map). Use the task-6 harness as the fast iteration gate.",
    "status": "done",
    "result": "28344 -> 9144 tris x4 variants (3.10x; lev[2].children 4->3, lev[2].continuation off, cards 30->9 @ cardSize 0.040->0.070, maxTris 30000->12000, configDeciduous only); per-variant total in [9144,9192] on ANY seed; A/B /tmp/treeopt_near_frame300.jpg matches baseline silhouette + canopy density (6-card start rejected as too sparse); in-game gpu 7.29->4.94 ms at frame 300, no new VUIDs, other species bit-identical."
  },
  {
    "id": 3,
    "desc": "Cut the far-LOD deciduous mesh (configDeciduousFar) to <=3500 tris (baseline 6688 = non-spine bands 1304 + spine block 1544 + cards 3840 = 160 tips x 6 cards x 4); screenshot A/B at 100-200 m (single-variant species, silhouette must hold on its own); optionally lower AZGAAR_PROPS_TREE_LOD_SWITCH in c-game/game/azgaar/AzgaarProps.cpp (currently 100 m; keep AZGAAR_PROPS_RESCATTER_DIST >= switch + LOD_MARGIN) if the far LOD holds at the shorter distance. Use the task-6 harness as the fast iteration gate (starting point: lev[2].continuation=false, lev[1].children 7->5, cards 6->3, maxTris 7200->4500; re-verify totals in the harness - the old analytic targets are void per the round-1 corrections). Round-3 anchor: near LOD is now 9 cards @ 0.070 (per-tip card area 0.0441) vs far 6 @ 0.058 (0.0202) - at 3 cards/tip the far LOD needs a cardSize bump (or 4-5 cards) for density parity; judge on the 100-200 m A/B vs /tmp/treeopt_baseline_frame300.jpg with the exact round-2 env vars (ENGINE_SCREENSHOT_FRAME=300, same map).",
    "status": "done",
    "result": "6688 -> 3152 tris (2.12x; lev[1].children 7->5, lev[2].continuation off, cards 6->4 @ 0.058->0.071, maxTris 7200->4500; [3152,3172] on any seed); A/B holds at 100-200 m (4-card chosen: 3-card @ parity failed clump solidity); LOD switch 100->70 m pulled (RESCATTER 100 >= 86), far holds at 86 m per-tree A/B, 70->60 left to task-4 profile; in-game 3152 bit-exact, no new VUIDs, pinned verification command printed VERIFICATION PASS (shot /tmp/treeopt_far4_frame300.jpg, switch shot /tmp/treeopt_far70_frame300.jpg)."
  },
  {
    "id": 4,
    "desc": "Re-capture with RenderDoc after the mesh changes (repro command and env vars in notes.md round 2) and compare via scripts/rdc_props_pass.py with the IDENTICAL method as the baseline: compare PASS SHARES and A/B deltas, never absolute ms (RADV pipelined timestamps overlap), and use the deterministic RasterizedPrimitives/SamplesPassed columns for draw-level conclusions; compare props-pass tri load against notes.md round 2 (color 5.76M submitted / 2.15M rasterized; shadow 15.64M tree-card tris); confirm the run log has no new VUIDs/warnings. Decide from this profile whether the LOD-switch pull (task 3 option) and shadow-pass approach C are warranted.",
    "status": "done",
    "result": "Identical-method re-capture (frame 300, same cell): props submitted 5.76M->2.13M (-63%), props rasterized 2.15M->0.82M (-62%), frame rasterized 9.36M->4.04M (-57%), shadow tree-card tris 15.64M->5.39M (-66%), wall gpu 7.29->3.87 ms; switch 100->70 isolated to 45 flips = extra 269,640 color + 808,920 shadow tris saved; 0 new VUIDs, WARN set byte-identical; both residual levers ruled OUT (70->60: 76-86 m band unquantifiable + far LOD untested at 76 m; shadow-C: headroom inside the \u00b125% replay-jitter band) - full A/B table + decisions in notes.md round 5."
  },
  {
    "id": 5,
    "desc": "Run the pinned verification command from plan.md (build + headless run + tri-count gate); it must print VERIFICATION PASS. No engine code changed since task 3's PASS (task 4 touched only scripts/rdc_props_pass.py LAYOUT data, not part of the build), so PASS is expected - triage and re-run only if the build or tri counts regress. Also write the rule-first lessons entry to docs/lessons/ for the non-obvious rules learned (last-level continuation spines at sub-pixel radius = invisible tri waste; cardCount is the canopy-SOLIDITY lever, cardSize the area lever; TAA run-to-run noise control ~1.8% px>30 sets the A/B-change threshold; RADV per-event GPU durations overlap - compare pass shares/A-B deltas, never absolute ms), and clean up the two ~600 MB /tmp/RenderDoc/*.rdc captures afterwards.",
    "status": "done",
    "result": "VERIFICATION PASS (exit 0) from the pinned command; no engine code changed since task 3 (build: ninja no-work), PASS not vacuous - headless re-run captured the azgaarPropMesh build line: deciduous/0..3=9144, deciduous_far/0=3152, conifer 307/310/316/352, mesh validation PASS (harness pre-check bit-exact; AzgaarProps SWITCH 70/RESCATTER 100/MARGIN 16 confirmed); run-log VUIDs (5x pre-existing VUID-00754 depthBiasClamp) and 16 WARNs byte-identical to /tmp/base_run.log; 4 rule-first lessons appended to docs/lessons/2026-09-09.md + docs/lessons.md index (continuation-spine waste; cardCount=solidity vs cardSize=area; TAA 1.8% px>30 noise floor; RADV overlapping event durations - shares/deltas only); /tmp/RenderDoc/*.rdc (1.2 GB) deleted; logs /tmp/treeopt_build.log + /tmp/treeopt_t5_verify_run.log."
  },
  {
    "id": 6,
    "desc": "Tri-counting harness: standalone tool under c-tools/ replicating the AzgaarPropMesh.cpp buildMesh variant loop, compiled against c-utils/treegen/TreeGen.cpp without linking the game; first run must reproduce the baseline.",
    "status": "done",
    "result": "c-tools/treegen_tricount/ (main.cpp + stub Utils.h + build_tricount.sh) reproduces the baseline exactly and deterministically (deciduous 28344x4, far 6688, conifer 307-352, etc.); established: 28344 is the FULL untruncated model (bands 9144 + cards 19200) and the near spine block = 7296 (not ~12k), far spine = 1544 - see notes.md round 1."
  },
  {
    "id": 7,
    "desc": "Remove Azgaar map rendering (plan.md): delete c-game/game/azgaar + loadingAzgaar, c-engine/ecs/system/heightmap, PropsRender + PropsRenderDiligent + HeightmapTerrainDiligent passes, the 7 heightmap/props .hlsl + pak copies + CMake rule, pak_1 azgaar/grass/leaf assets, make_leaf_sprite.py; strip Game.cpp world wiring (props bridge, look registration, map-derived spawn/vantages), PlayerGui cell text, PlayerActionsGui cell teleport, MainMenuGui heightmapTerrainSystem, Player ground-snap gate, shadow-pass terrain/props draws; update docs (env.md, renderdoc-capture.md) + plans status notes. Build + headless verification.",
    "status": "done",
    "result": "Full Azgaar world removed (azgaar/ + loadingAzgaar/ + ecs/system/heightmap + PropsRender/PropsRenderDiligent/HeightmapTerrainDiligent passes + 7 hlsl + pak copies + pak_1 azgaar/grass/leaf assets + make_leaf_sprite.py); Game.cpp world wiring, PlayerGui cell text, PlayerActionsGui cell teleport, MainMenuGui heightmapTerrainSystem, Player ground-snap gate, shadow-pass terrain/props draws + ShadowPassAttribs cbuffer all stripped (PBR receiver cascade pick re-sourced to playerGetFootPos); CMake hlsl rule reduced to rmlui pair; docs/env.md + renderdoc-capture.md + plans status notes updated. Build clean (one stray groundY fixed), pak_1 99 M with zero azgaar entries, grep sweep clean. Headless verification: world run + rmlui menu->world run both exit 0 with 0 VUIDs and no new warnings; RenderDoc frame shows shadow/player/taa_resolve pass set, character rasterized at 60m+ and in shadow pass. Found + logged (docs/lessons/2026-09-10.md) a PRE-EXISTING PBR lit-pass close-range clip (rpr=0 within ~20m of camera, rpr=23111 at 60m+) unrelated to the removal; /tmp/RenderDoc captures deleted."
  }
]