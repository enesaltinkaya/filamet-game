# plan

The splat-terrain shadow *receive* path was hard-gated to PCF (mode 1), so all filterable modes (2=VSM, 3=EVSM2, 4=EVSM4) rendered no ground shadows — and a complete fix already sits uncommitted on baseline bb7d9e0 (linear `g_ShadowMapLinear` slot + `g_ShadowMapLinearSampler`, `filterShadowVSM`/`filterShadowEVSM` branches in `splat_terrain_ps.hlsl` on raw cascade z with no `fFixedDepthBias`, mode passed via `f4ShadowFade.y`, `ENGINE_SHADOW_MODE` env override in Renderer.cpp, DebugGui 0-4 toggle). Strategy: treat the working tree as the fix candidate and prove it end-to-end rather than rewriting — boot modes 0-4 headless with a pinned teleport (`180,640,180`, puts the player caster in the ~60 m shadow tier) and the attenuation overlay (`ENGINE_SPLAT_SHADOW_DEBUG=2`), then judge per-mode mask parity against the PCF baseline: each filterable mode must show a dark terrain shadow region of the same shape/extent as mode 1 (softness allowed), mode 0 shows none, no acne/noise in lit areas, zero VUIDs. Any failing or marginal mode gets a RenderDoc atlas dump (`scripts/run.sh renderdoc` + `rdc.py dump shadow|world`) to separate generator from receiver, with bias/exponent constants in `ShadowDiligent.cpp` as the only tuning knobs; the glTF player receiver is PCF-only by design and out of scope. Finish with an un-overlaid mode-1 screenshot to confirm the shipping look is unchanged and `settings.json` still at `shadowMode=1`.

Verification: ./scripts/build.sh && TERM=xterm-256color ENGINE_AUTOTEST=enter ENGINE_HIDDEN_WINDOW=1 VK_ICD_FILENAMES=/usr/share/vulkan/icd.d/radeon_icd.json ENGINE_SHADOW_MODE=2 ENGINE_TELEPORT=180,640,180 ENGINE_SCREENSHOT=/tmp/shadow_verify.jpg ./build/c-game/c-game
