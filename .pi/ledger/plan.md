# plan

The user reports ground shadows only under PCF; prior investigation (see notes carry-over) found the non-PCF *receive* path in the splat terrain PS was never implemented (hard-gated to mode == 1, dummy texture bound for VSM/EVSM), and a full implementation now sits uncommitted in the working tree (linear `g_ShadowMapLinear` slot, `filterShadowVSM` / `filterShadowEVSM` branches, `ENGINE_SHADOW_MODE` env override, DebugGui 0-4 cycle). Strategy: treat the working tree as the fix candidate and verify it end-to-end — build, boot every mode 0-4 via `ENGINE_SHADOW_MODE`, confirm with the attenuation overlay + teleported-caster screenshots that terrain+player ground shadows appear in modes 1-4 (parity with PCF), and that PCF is not regressed and no Vulkan validation errors fire. Approach: one verification round per remaining doubt (mode coverage, overlay parity, PCF regression), fix any mode that fails its parity check, and keep the final settings.json at shadowMode=1.

Verification: ./scripts/build.sh && TERM=xterm-256color ENGINE_AUTOTEST=enter ENGINE_HIDDEN_WINDOW=1 VK_ICD_FILENAMES=/usr/share/vulkan/icd.d/radeon_icd.json ENGINE_SHADOW_MODE=2 ENGINE_TELEPORT=180,640,180 ENGINE_SCREENSHOT=/tmp/shadow_verify.jpg ./build/c-game/c-game
Baseline commit: bb7d9e082fada66058d7d963f6ca14769b7f4f2d (dirty — uncommitted prior-run fix in c-engine/renderer/Renderer.cpp, c-engine/renderer/diligent/SplatTerrainDiligent.cpp, c-game/data/pak_1/materials/splat_terrain_ps.hlsl, c-engine/gui/rmlui/guis/DebugGui.cpp, plus AGENTS.md and .pi/ledger)
