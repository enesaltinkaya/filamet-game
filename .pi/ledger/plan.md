# Plan

The cascade shadow path is `c-engine/renderer/diligent/ShadowDiligent.{h,cpp}` (DiligentFX shadow FX, `shadowMode` 0=off..4=EVSM4, `shadowQuality` 0=low..2=high mapping to cascade count/atlas size); settings are read from `build/c-game/data/settings.json` via `utils::settingsGetInt`. The working tree is dirty from a prior "terrain shadows" run (ShadowDiligent, DiligentRenderer, SplatTerrainDiligent, Player.cpp modified) — that prior run's notes are gone from the ledger, so workers should re-derive context from code, `docs/lessons.md`, and fresh diagnostics rather than assume anything is broken there. Strategy: (1) run the quality sweep screenshot and look at the player-shadow visibility per quality level; (2) use RenderDoc per `docs/renderdoc-capture.md` (the `shadow` debug group, `scripts/rdc.py`, plus the in-engine `ENGINE_SHADOW_READBACK` / `ENGINE_SHADOW_DUMP` env hooks in ShadowDiligent.cpp) to inspect cascade frustum coverage, player depth landing in the atlas, and sampling matrices at each quality; (3) check the classic failure fingerprints: player outside cascade coverage / cascade-split seams, W2L matrix handedness or flip-Y mistakes, cascade selection (farther cascade = coarser, player may be in a tiny UV region), PCF/bias values, atlas size vs quality mapping; (4) fix and re-run the sweep. Fix must be minimal and scoped to the shadow path unless diagnosis proves otherwise.

Verification: scripts/build.sh && for q in 0 1 2; do python3 -c "import json; p='build/c-game/data/settings.json'; d=json.load(open(p)); d['shadowQuality']=$q; d['shadowMode']=1; json.dump(d,open(p,'w'),indent=4)"; env ENGINE_HIDDEN_WINDOW=1 VK_ICD_FILENAMES=/usr/share/vulkan/icd.d/radeon_icd.json ENGINE_AUTOTEST=enter ENGINE_AUTO_RUN=1 ENGINE_SCREENSHOT=/tmp/shadow-q$q.jpg ENGINE_LOG_TIMEOUT=15000 ./build/c-game/c-game; done

Baseline commit: 4669eee3bdb9515311bd7e23510ddda942464fd9 (dirty)

Notes for workers: `ENGINE_LOG_TIMEOUT` is milliseconds (game also exits on its own after the screenshot; the timeout is a safety net). Screenshot fires ~100 frames in (`ENGINE_SCREENSHOT_FRAME` to override). Prior-run leftovers: `/tmp/tshadow-*.jpg` may exist. A prior run pinned the player via `build/c-game/data/db/db.db` (sqlite, table `player`, packed 8 floats) and used an `ENGINE_CAMERA=cast` vantage; `ENGINE_SHADOW_NO_PLAYER` exists as an A/B control. All `ENGINE_*` env vars require `export` or inline `env`.
