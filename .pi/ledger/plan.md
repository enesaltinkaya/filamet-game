# Plan

Strategy (corrected by brainstorm): DiligentFX's SSR is `PostProcess/ScreenSpaceReflection` (NOT Hydrogent — Hydrogent is a USD renderer; the earlier draft was wrong). `c-engine/renderer/diligent/TaaDiligent.cpp` already creates a DiligentFX `PostFXContext` and calls `postFXContext->Execute` each frame with camera CB, double-buffered depth SRVs and motion SRV — the exact inputs SSR needs. So: (a) add an isolated terrain gbuffer pass (new `SsrDiligent.cpp`) that draws the terrain into one RGBA16F texture (xyz = world-space normal, a = roughness; cleared to (0,0,1,1) so props pixels don't spawn rays), using a macro variant of `heightmap_terrain_ps.hlsl` that early-outs after the normal/roughness chain, with `taaDepthDSV` bound read-only, run after the props pass; (b) create a `ScreenSpaceReflection` object and run `PrepareResources`/`Execute` inside `taaWorldResolve` right after the existing `postFXContext->Execute`, gated behind `ENGINE_SSR=1`; (c) composite its radiance output into the final blit step as `src.rgb + ssr.rgb * ssr.a`; (d) temporarily force terrain roughness low (~0.1) in the terrain PS so SSR is visibly glossy — record the original lines in notes for revert. No new library work: `libDiligentFX.a` is already built and linked; FX shaders come from the in-archive source factory. No comments in new code (AGENTS.md). Keep every change reversible.

Verification: ./scripts/build.sh
Baseline commit: 5b7c14f3dfc7e647a8ba660d79c683509ac08db1 (dirty)
