[
  {"id": 1, "desc": "Recon: document how to mirror the Filament terrain pass into the Diligent backend (files, PSO/SRB patterns, texture path, dispatcher call sites)", "status": "done", "result": "Recon written to docs/azgaar-terrain/phase6-diligent-recon.md; deviation: runtime HLSL compile at pass init (glslang can't parse HLSL); c-utils Image::mipSizes bug noted"},
  {"id": 2, "desc": "HLSL VS/PS mirroring terrain.mat (thin VS, full-look PS w/ DiligentFX PBR lighting includes)", "status": "done", "result": "shaders/heightmap_terrain_{vs,ps}.hlsl created; both compile cleanly to SPIRV under the Diligent include env (verifier PASS; runtime CreateShader path still to be proven at init)"},
  {"id": 3, "desc": "HeightmapTerrainDiligent.{h,cpp}: pass init, per-READY-tile VBO upload + draw, readyStamp cache, LRU eviction, deferred destroy, stats + CMake copy step for .hlsl", "status": "done", "result": "Full pass implemented (~1000 lines) using the 2026 Diligent API (PRS/SRB/CommitShaderResources); shared SRB with per-tile SetVertexBuffers; CMake copies .hlsl into pak_1; build clean (verifier PASS); runtime CreateShader + boot not yet exercised"},
  {"id": 4, "desc": "Wire terrain pass into Diligent backend + dispatcher; setWorldDrew(true); verify build + ENGINE_SCREENSHOT (seams, look, streaming)", "status": "in-progress", "result": "Wiring done (dispatcher 6 sites, draw/destroy ordering, setWorldDrew, c-game ktx/stb link); build + boot verified PASS; OPEN BUG: terrain renders misplaced — thin distant band (default cam), blank (top-down)"},
  {"id": 5, "desc": "Fix geometry misplacement: GPU-side VS input (Vulkan input layout 'ATTRIB' semantics / LayoutElement offsets) vs immutable VBO upload; dump one tile's GPU VBO to distinguish, then fix", "status": "pending", "result": null}
]
