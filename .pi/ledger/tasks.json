[
  {"id": 1, "desc": "Audit grass card PNGs: per-file IHDR color-type/bitdepth, PLTE+tRNS, decoded min/max alpha — new pak AND old-engine copies.", "status": "done", "result": "All 7 PNGs (new+old, byte-identical md5) are real 8-bit RGBA cutouts (minA=0/maxA=255, 44-70% zero-alpha). Asset regression disproven — defect is runtime-side."},
  {"id": 2, "desc": "Compare vs old engine grass rendering for intended behavior", "status": "done", "result": "merged into task 1 (old-engine copies included in the audit)"},
  {"id": 3, "desc": "Fix the runtime alpha loss: bind cardTex with a verified-clean sampler config (LINEAR mag + NEAREST min).", "status": "done", "result": "cardTex now binds samplerCardClamp (NEAREST min / LINEAR mag / CLAMP) in PropsRenderFilament.cpp; rebuilt; verifier PASS — no flat patches at user vantage, tufts unchanged, close-up correct."},
  {"id": 4, "desc": "Harden PropsRenderFilament::loadGrassTexture: min/max-alpha validation with utils::warn for opaque cards, replace dead channels!=4 guard, log resolved/fallback texture per variant", "status": "not-started", "result": "skipped — optional hardening, not part of the reported defect (early sign-off per rules); assets audited healthy, fix verified"},
  {"id": 5, "desc": "LOCATE runtime alpha loss (upload format, sampler/mip, meshB.w gating).", "status": "done", "result": "RADV (Mesa 26.2.1 NAVI31) returns OPAQUE alpha when LINEAR-minifying an SRGB8_A8 texture → discard never fires on minified (mid-distance) cards → solid striped tinted rectangles. Upload, discard gating, bias=0 all exonerated. A/B matrix in notes.md round 2."},
  {"id": 6, "desc": "Decide fate of round-2 env diagnosis hooks", "status": "done", "result": "stripped both (ENGINE_PROPS_CARD_SAMPLER / ENGINE_PROPS_CARD_FORMAT) — a knob that can re-select the broken LINEAR-min config is a liability; matrix preserved in notes.md"}
]
