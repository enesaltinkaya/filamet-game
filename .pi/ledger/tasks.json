[
  {"id": 1, "desc": "Build + boot sweep: modes 0-4 via ENGINE_SHADOW_MODE with ENGINE_TELEPORT=180,640,180 + ENGINE_SPLAT_SHADOW_DEBUG=2 overlay, screenshots /tmp/shm<N>.jpg, check `shadow: mode N` log lines and zero VUIDs; then mask-parity of modes 2-4 vs mode 1 (same shape/extent dark region, no acne)", "status": "pending", "result": null},
  {"id": 2, "desc": "Fix any mode failing parity/regression found in round 1 (PCF must stay identical to baseline behavior); RenderDoc atlas dump of the failing pass if receiver vs generator is ambiguous", "status": "pending", "result": null},
  {"id": 3, "desc": "Final pass: clean build + un-overlaid mode-1 screenshot, settings.json at shadowMode=1, no VUIDs, report verdict", "status": "pending", "result": null}
]
