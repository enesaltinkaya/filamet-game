[
  {"id": 1, "desc": "Inspect old engine rmlui gui layer (GuiManagerRmlUi, guis, crmlui usage, CMake link lines) and new engine gui/renderer/ECS conventions; note integration points in notes.md", "status": "done", "result": "Done in brainstorm pass: crmlui C surface is small (rmlInitVulkan(RmlParams*) + rmlUpdate/rmlRenderVulkan + doc API); half the glue exists in new engine (DataCallbacks match, rmlui docs already in c-engine/data/pak_0_engine/gui, guiDraw hook, GuiDiligent as dynamic-buffer template); Lua mandatory; render callbacks must queue CPU-side and draw in renderer hook (approach A, Diligent-native pass)."},
  {"id": 2, "desc": "Add crmlui wrapper + librmlui* libs + liblua to CMake linking and include paths (c-engine/c-game, --start-group, platform dirs, InputEventShared include)", "status": "done", "result": "VERIFIED PASS: crmlui+librmlui*+liblua+libfreetype linked into c-game --start-group, include dirs added to c-engine (crmlui.h resolves both styles). Build green via ./scripts/build.sh."},
  {"id": 3, "desc": "Port LuaSystem from old engine (c-engine/ecs/system/lua) + register lua C bindings (hover/click/exit) + Window cursor support + per-frame InputEvent shim from engine::input", "status": "done", "result": "VERIFIED PASS: LuaSystem ported (lua_State, hover/click/exit bindings, registered priority 2), Window cursor API (SDL system cursors, set/visible query), per-frame InputEvent shim in windowPollEvents. NOTE: lua 5.4 headers must be included in extern C from C++. Shim collapses to 1 key-down/up + 1 mouse button per frame (fine for GUI)."},
  {"id": 4, "desc": "Implement Diligent rmlui render pass (RmlParams.vulkan callbacks: CPU geometry pool, per-frame dynamic vertex/index buffers, per-texture batches, scissor per batch, ortho UI HLSL shader via pak-ship pattern) + rmluiDraw(context) hook beside guiDraw in DiligentRenderer::draw. Read docs/lessons.md dynamic-buffer clobbering first.", "status": "done", "result": "VERIFIED PASS: RmluiDiligent pass + smoke system working. RmlParams owned as system member (dangling ptr fixed). Screenshots show RmlUi doc rendering correctly (text/images/colours), build green. Key rules: own RmlParams storage; y-down GLM ortho + transpose; per-texture SRBs; own vkCmdBegin/EndRendering scope; rmlDestroy while lua alive. Smoke system retired in task 5."},
  {"id": 5, "desc": "Port GuiManagerRmlUi.{h,cpp} adapted (full RmlParams init, deferred add/remove, resize rmlSetDimensions, guiManagerIsMouseInteracting gate, settings/env toggles) + engine guis (ShowFps, Debug, PassStats, Stats) with GPU-time bindings stubbed where missing", "status": "done", "result": "VERIFIED PASS: GuiManagerRmlUi (owns RmlParams, input pump, cursors, resize, toggles, deferred add/remove) + all 4 engine guis ported; smoke system retired; screenshots show manager-driven rmlui guis rendering (debug/passstats/stats/showFps). Stubs where Diligent engine has no equivalent (GPU per-pass times). Fixed old-engine bug: debugReleaseText bound as char** not bool."},
  {"id": 6, "desc": "Verify build via ./scripts/build.sh and screenshot smoke test: rmlui document renders (ENGINE_SCREENSHOT), no crash at startup", "status": "done", "result": "VERIFIED via round-5 runs: build green, screenshots show all four manager-driven guis + showFps rendering, clean RML SHUTDOWN teardown."},
  {"id": 7, "desc": "Integration decision: port old pak_1/gui game-level rmlui guis (pauseMenu, playerActions, zone, settings...) vs keep ImGui guis side-by-side - scope with user once engine rmlui path is green", "status": "pending", "result": null}
]
