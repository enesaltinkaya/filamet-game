# 2026-09-13

- A "camera-distance" value read from the TRANSPOSED runtime matrix is a different axis entirely: `diligentFrameView()` is the runtime-glslang transposed view, so the focus-band / cascade-pick code reading `view._31.._33` got the true view's third COLUMN (a near-perpendicular dot → focusCamZ ≈ 0.07 instead of 10.85) — cascade 0 stayed at the raw log split (0–4.7 m, pure sky at third-person vantages), every ground pixel fell to the 80 m cascade (~8 texel/m, the player's whole shadow ≈ 3 texels) and PCF erased it. Fingerprint: `shadow trace ... band 3.1` constant while the player sits ~11 m out, and `ENGINE_SPLAT_SHADOW_DEBUG=3` all-magenta (c0 uv-branch never runs) while `ENGINE_SHADOW_ORACLE` passes (it never tests camZ). Fix: read the true z row from the transposed matrix (`view._13/_23/_33`); same fix in the PBR receiver cascade pick.
- Automated runs must never write the state db: the menu-enter flow (ENGINE_AUTOTEST=enter) ends with the player/orbit ACTIVE, the character slides off her parked spot, and `PlayerSystem::postUpdate`'s 1 Hz periodic saves overwrite BOTH the `player` row (her slid position) and the `camera` row (the orbit eye) — one screenshot run is enough to destroy a parked setup. Fingerprint: `flying camera: loaded saved state pos (...)` logging coordinates you never parked. Fix: gate the periodic saves with `automatedRun()`; restore clobbered rows from a pre-session byte dump (capture `select data from camera/player` BEFORE the first automated run of a session).
- "Shadow invisible" at a known-good parked vantage is a CONTRAST bug, not a caster bug — measure before touching the shadow pipeline: `ENGINE_SPLAT_SHADOW_DEBUG=2` (attenuation to screen; blob present ⇒ caster+receiver fine) then a scanline brightness profile across the shadow in the beauty frame. This engine is IBL-dominated (env carries the sun disk): direct sun adds only ~20% over ambient, so a PCF-correct shadow was a ~10% sRGB dip — unreadable on scree albedo. Mitigated with ambient-in-shadow darkening (`f4ShadowFade.w`, `ENGINE_SHADOW_AMBIENT_DARKEN`, default 0.5), not a sun/IBL rebalance.
- Automated runs skip the saved-camera restore by design (scripted vantages own the view); `ENGINE_CAMERA_DB=1` opts back into the parked camera row for screenshot verification.
- A "shadow" that attenuates identically to 1.0 everywhere in `ENGINE_SPLAT_SHADOW_DEBUG=2` is NOT the shadow map — park the suspicion in the material chain before touching CSM: the crest dark band at the cliff vantage was `wCliff` (scree ramp) painting partial triplanar scree over the sun-facing shoulder, not a shadow term. Proof stack: attenuation≡1 → albedo-debug shows the band → beauty/albedo ratio map is flat → slope/wCliff debug shows the band = ramp zone. Fingerprint: band hugs a material boundary, brightness delta ~10 %, detail texture fades inside it.
- Terrain PS debug modes are fed by `dbg*` variables that must be ASSIGNED inside the cascade loop — they were declared-only, so modes 1/3/4/5/6 printed initializers forever (debug 3 all-magenta, debug 5 all-black even on shadowed ground). `dbgZ=viewZ*0.0025, dbgRaw=att, dbgCascade=cascade` now set per sample; mode 7 = `(slope, wCliff, wSand)`.
- Debug colors go through the tonemap/post chain: display ≈ `1-(1-raw)³`, calibrated by the normals-view anchor (raw 0.5 → display 0.89) and attenuation (raw 1.0 → 1.0). Invert (`raw = 1-(1-d)^⅓`) before comparing debug screenshots numerically — reading display values as raw made slope 0.36 look like 0.71 and nearly shipped cliff thresholds 2× too steep (wiping the scree face).
- The orbit vantage drifts a few px between runs — single-pixel probes across two screenshots near a slope gradient contradict each other; compare structure (silhouette rows, ratio maps) or the same debug mode, never cross-run single pixels.
- Terrain CSM receive had regressed from the 2026-09-12 landed fix: `break` after the first covering cascade (kills tall-caster composition through cascades) and `viewZ > zend → continue` for ALL modes (kills PCF sampling 80 m → box edge, which the 223 m `f4ShadowFade.x` fade was sized for). Restored: multiply every in-box cascade, zEnd gate only for the filterable modes (VSM/EVSM Chebyshev reads bogus past the stored range); PCF past the stored depth compare-vs-clear → lit naturally.
- Splat cliff band thresholds landed at `SPLAT_CLIFF_LO 0.2 / SPLAT_CLIFF_HI 0.32` (raw slope; was 0.1/0.4): the old 5.7°–21.8° ramp smeared partial scree over every convex shoulder; env overrides `ENGINE_SPLAT_CLIFF_LO/HI` still tune without a repak.
- Back-slope acne (triangle-level attenuation moiré dipping to ~0.85 on sun-away slopes) = the terrain caster PSO shipping `SlopeScaledDepthBias 0` while `plans/terrain-shadow.md` always specified "default 2.0 texels" — `shadowDiligentCasterBias` now lands 2.0 (`ENGINE_SHADOW_SLOPE_BIAS` still overrides, 0 disables). Fingerprint: checker visible only where the terrain samples shadows (closer than the box reach), clean in `ENGINE_SHADOW_MODE=0`, clean on sun-facing slopes; `ENGINE_SPLAT_SHADOW_DEBUG=2` shows it as per-triangle attenuation dips with no occluder present. Attached shadows survive (no peter-pan at 2 texels).
- Screenshot runs against an edited binary REQUIRE `scripts/build.sh` — running `build/c-game/c-game` directly renders the stale binary and an A/B "fix" can be pure imagination; compare regions numerically (G-mean/std over a fixed crop) instead of eyeballing two near-identical JPEGs.
