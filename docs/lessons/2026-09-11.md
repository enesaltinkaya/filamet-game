# 2026-09-11

- Diligent `float4x4` is ROW-VECTOR convention (translation in `_41.._43`, stored 3x3 = S·Rᵀ) — decomposing its COLUMNS with the standard row-major matrix→quat formula yields the INVERSE rotation (position and scale still come out right, and an AABB check cannot catch it because ±θ about one axis share an AABB). Fingerprint: props physics body that is "scaled/offset wrong" while its JOLT_DEBUG world-bounds print matches the render exactly, phantom collision ~10 m in front of the visible surface, body rot quat = glTF quat with the Y (or axis) component sign-flipped; incident: player blocked ~10.5 m before the big rock (`vjrmfb1ab..._0.002`, node 319, MESH shape, scale 0.334, Y-rot −15.68°) — fixed by replacing the hand-rolled Gram-Schmidt+quat extraction in `gltfPropsNodeGlobalTRSDiligent` with Diligent's `Matrix4x4::Decompose()` (row-convention aware, handles mirror via negative scale)
- Row-vector global composition is `global = local * parent` (Diligent's `UpdateNodeGlobalTransform`: `GlobalMat = LocalMat * ParentMatrix`) — hand-rolled parent walks that left-multiply (`g = parentLocal * g`) scramble any chain with parent ROTATIONS while pure-translation chains (flat hierarchies) commute and look fine. Fingerprint: body pos = (x, −z, y) of the expected position, only nodes under rotated parents affected (FAB sandbag pile's ±90° X pair), trees/rocks with near-root parent chains correct; incident: FAB trench child-item bodies at (x, −z, y) — fixed by flipping to `g = g * parent->ComputeLocalTransform()` to mirror `UpdateNodeGlobalTransform`
- Numeric probe beats bound checks: a body's `GetWorldSpaceBounds` can match the render to 0.1 while its collision surface sits 10 m away — compare a RAYCAST distance (physics) against an analytic mesh raycast (render triangles) at the same origin/direction; harness: extract the sidecar blob entry, `joltCreateBodyFromShapeBlob` + `joltCharacterCreate` in a 30-line linked-against-cjolt main, drive the wrapper's env-gated `JOLT_TEST_CAST`/`JOLT_TEST_POS` probes (`JOLT_DEBUG=1`); body-id prints are index-only — lock by `GetIndexAndSequenceNumber()` (sequence ≠ 0)
- Terrain POM needs the height channel to exist END-TO-END: the 8 splat detail normal.ktx2s shipped with FLAT A=255 (the old-engine height-in-normal-alpha convention was only populated for the 4 band materials grass/sand/snow/cliff), so the ported POM march hit a constant field and degenerated to an invisible uniform UV shift — a shader-side A/B "differs, no artifacts, GPU cost up" is NOT evidence of working POM. Fingerprint: POM visible only on unpainted base/band regions, painted regions flat, march early-outs (sampledHeight ≥ 1.0 at start); verify the texture alpha histogram (stdev ≈ 0 = no data) before debugging the shader; incident: parked-player spot (mud/roads1 paint) showed no parallax; fixed by baking the source displacement maps (PNG/JPG/EXR — EXR via cv2, fallback `magick -auto-level`) into normal.ktx2 alpha in `blender-terrain.py --details-only` (`findDisplacementMap`/`loadHeightMap`); `rocky_terrain_02_disp_4k.png` is all-white in the local asset copy, so the unpainted base stays flat until that asset is re-fetched
