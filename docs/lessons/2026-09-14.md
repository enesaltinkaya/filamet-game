# 2026-09-14

- Straight-edged bright/dark trapezoid on a TALL VERTICAL prop that (a) slides when the camera slides and (b) jumps or vanishes when the cascade focus band moves (`ENGINE_SHADOW_FOCUS_MARGIN` A/B) is the camera-anchored cascade slab boundary slicing the prop — decide this BEFORE touching receiver math: same camera restore (`ENGINE_CAMERA_DB=1`), one run per margin value, diff the same pixels. Fingerprint: parked player before test2's big white walls, PCF q1 (2048², 2 cascades, band 14 m), left panel split by a hard diagonal step 0.55→0.44 linear at cam-z ≈ 17–18 m with a partial-PCF recovery ramp behind it; RenderDoc eid 815 D32 slice 1 holds NO wall silhouette at the wall's own light-space UVs (stored depth ≈ terrain, off the wall's own depth by metres); normals + albedo identical across the step (not geometry, not material). Band 12 and band 32 runs render the wall clean, band 14 breaks it.
- The step is NOT explained by the receiver's zend-only gate alone: adding the zstart gate (`fCameraViewZ < zstart → continue`) to the props union loop in `PBR_Shading.fxh` changed nothing visible (failing region extends INSIDE c1's slab, where the wall's own caster depth should win the compare) — reverted. CPU reproduction of `FilterPropsShadowCascade` from the frame-600 logged matrices says LIT at every wall sample including the visibly darkened ones — some convention in the light-UV path (stored matrix orientation vs `mul(float4(p,1),M)` vs the D32 readback layout) still mismatched; do NOT trust hand-rolled light-UV replication, validate against a visually-known shadow (e.g. the sacks' ground shadow) first — that check alone exposed the mismatch here.
- Next-session leads: dump the props caster's actual per-cascade draw matrices + the wall mesh's c1-rasterized footprint (probe texels around (719–799, 1116–1187) of slice 1 at frame 600); suspect the caster side (stale/wrong matrix or anchor for the wall's mesh in c1) over the receiver side — `ENGINE_SHADOW_ORACLE` only proves CPU matrix agreement, not which texels a given mesh actually wrote. Engine debug hook added then reverted (comments policy): frame-targeted `shadow dbg` dump in ShadowDiligent.cpp — re-add if the matrix dump at a late frame is needed again.
- Incident: "what is this shadow glitch on the white wall" — the grey trapezoid on test2's left wall panel east of the parked player, present at every quality tier boundary crossing the panel, misread initially as cast-shadow/acne.
