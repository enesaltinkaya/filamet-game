# 2026-09-10

- A pass that runs its VS work but rasterizes zero primitives is a CLIP-TRANSFORM problem, not a "draw missing" one — measure it (RasterizedPrimitives counter + post-VS clip data), don't re-derive the math on paper: the PBR lit pass clips the character when it sits within ~20 m of the camera (rpr=0, VSInvocations 17015, offscreen RT uniform clear) while the same mesh rasterizes 23111 tris at 60 m+ AND rasterizes fine in the SHADOW pass at every distance (sps>0) — the close-range failure was found while verifying the azgaar removal (ENGINE_CAMERA=character over a worldless origin spawn); cause unresolved, PBR/camera/placement/TAA paths untouched by that change — don't build close-range validation vantages on the lit pass until this is understood
- Removing a world render pass also removes hidden consumers of its state: the shadow module's per-cascade cbuffer (ShadowPassAttribs) + ENGINE_SHADOW_NO_TERRAIN/PROPS/CULL flags existed only for the world shadow draws, but the PBR RECEIVER cascade pick read the player position from the props pass' wind-state getter — re-source player state from engine::playerGetFootPos when the props pass goes away
